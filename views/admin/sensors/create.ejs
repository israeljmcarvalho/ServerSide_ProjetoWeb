<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sensores</title>
  <%- include('../../imports'); -%>
</head>
<body>
  <%- include('../../navbar'); -%>

    <div class="container">

        <div class="row">
                <div class="col-md-12">
                    <h1 id="id_create_page">Novo Sensor</h1>
                </div>
        </div>
         
        <div class="row">

            <div class="col-md-12"> <!-- Parametrizado para ocupar 12 unidades de largura -->

                <form action= "create" method= "POST" enctype="multipart/form-data">  <!--acrescentado o comando enctype="multipart/form-data" para poder ser enviado imagem também-->
 
                    <div class="form-group">  <!-- No Bootstrap chamamos de "form-group" as subareas... neste exemplo a subaria é composta por 1 label, e imput e um "minilabel" para ser utilizado como texto de ajuda  -->
                        <labels for="sensor_Name">Nome</label>  <!-- Este campo FOR serve para linkar o label com o imput (perceber que o texto to FOR é o mesmo do ID do Imput) para quando a pessoa clicar no Label o cursor do mouse ir direto para o imput -->
                        <input name="sensor_Name" type="text" class="form-control" id="sensor_Name"  placeholder="Nome do Sensor"> 
                                <!-- o parâmetro NAME acima refere-se ao nome dado para linkar com o controler para buscar o título do sensor. Pode ser dado
                                 qualquer nome, neste caso foi utilizado a string "title". Este "name" não é ID do campo, ele vai dizer para o servidor como é que
                                 veio cada informação -->
                        <br>
                        <labels for="sensor_Temperature">Temperatura</label>
                        <input name="sensor_Temperature" type="text" class="form-control" id="sensor_Temperature"  placeholder="Informe a Temperatura"> 
                        <br>
                        <labels for="sensor_Humidity">Umidade</label>
                        <input name="sensor_Humidity" type="text" class="form-control" id="sensor_Humidity"  placeholder="Informe a Umidade"> 
                        <br>
                        <div class="form-group"> 
                            <label for="sensor_Image">Imagem</label>
                            <input name="sensor_Image" type="file" class="form-control" id="sensor_Image">
                        </div> 
                    </div> 

                    <button type="submit" class="btn btn-primary">Salvar</button>
                </form>
          </div>
        </div>  
    </div>

</body>
</html>